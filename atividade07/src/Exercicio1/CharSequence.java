public interface CharSequence {

    //Criando assinaturas da interface
    char charAt (int index);

    int length ();

    String toString ();

    CharSequence subSequence (int start, int end);
}

